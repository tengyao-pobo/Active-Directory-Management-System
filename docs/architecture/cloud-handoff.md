# Cloud development handoff — 2026-09-12

## Local development resumed — 2026-09-12

The user cancelled cloud development and requested continuous local development again. Local work resumed from remote head `9f0ebd4efd110efbe0a30d144d0d4bc7cd080db0`, preserving all four cloud commits. The old cloud task was unavailable through both task lookup and its browser URL; no claim is made that a stop operation succeeded. GitHub remains the source publication and CI destination.

Local locked restore and all 109 delivery unit tests passed. After restarting the project-only PostgreSQL instance on its original loopback port, 48 delivery integration tests passed. The next bounded repair replaces count-only delivery policy/trigger checks with exact relation, policy, expression, role and trigger metadata sets. Executing the installation slice found that `mint_permits` has no `environment_id`; its policies now use its globally unique operation foreign key, consistent with the pinned operation/environment validation in the helpers. Fifteen new rollback-only PostgreSQL tests pass: fourteen structural drift cases and exact permit operation-context visibility.

The new structural check deliberately rejects the current incomplete profile: `Principals` and `Sessions` still have RLS disabled. The catalog test enables their RLS flags only inside its rollback fixture; this is not a production policy migration. An additional unprefixed guard-reuse negative brings the structural/permit suite to 16 tests. The prior full-backend snapshot passed all 14 projects (1,532 tests, including 582 integration tests), with zero build warnings/errors; that snapshot predates this sixteenth case and the identity increment below.

`build/enrollment-delivery-identity.sql` is now staged source only and is NOT included by any installer. Its zero-argument owner-definer API-session attestor has explicit PUBLIC EXECUTE, returns only a boolean, and checks every Api binding component. Direct API policies also require CURRENT_USER=SESSION_USER; separate plan-lock policies preserve row locks while `WITH CHECK(false)` rejects actual principal changes. Execution gets command-specific SELECT/UPDATE policy pairs, not a DELETE-capable ALL policy. Seven rollback-only identity tests cover invalid binding shapes, API session CRUD/touch/step-up/revoke, the real plan-lock helper, unrelated-definer isolation, denied principal changes/role switching, and scoped execution row visibility. The executor probe deliberately bypasses the complete function inventory and does not prove runtime authorization. The 71-case delivery suite passed before the final narrowing of identity policies; the seven affected identity tests passed again after that narrowing.

The next local checkpoint adds the unconsumed catalog-only `build/audit-enrollment-delivery-identity.sql`: exact policy sets, forced RLS ownership, helper body/metadata/ACLs (including no planner support hook), role separation and fixed catalog search path. Seventeen catalog drift cases pass. The eight identity behavior cases now include maintenance as a non-superuser, non-BYPASSRLS owner. An isolated disposable database also passes real HTTP session read/touch/logout and subsequent rejection; this does not cover WebAuthn login/assertion or step-up completion. The combined delivery integration suite passed 90 tests before the runtime privilege increment below. Daybreak reviewed these bounded source and fixture changes without approving full activation.

The draft global delivery runtime privilege slice now checks every reserved delivery runtime against application relations in every non-system schema, including effective PUBLIC/column grants, PostgreSQL 18 MAINTAIN and sequence privileges. It also rejects database/schema CREATE, future table/sequence default grants and missing/mismatched role identities. It excludes ordinary system-catalog reads instead of rejecting valid runtime roles for them. Thirteen rollback-only PostgreSQL cases cover this slice, including an extra runtime's drift. This is a source-slice test, not proof that complete runtime provisioning succeeds.

`build/audit-enrollment-delivery-bindings.sql` is another unconsumed catalog-only slice. It pins the five columns, eight PostgreSQL 18 constraints (including three NOT NULL catalog entries), LoginRole primary key, foreign keys, purpose/shape checks, and all four indexes with raw keys/opclasses/collations/primary flags. It rejects incoming foreign keys, extra policies/rules/indexes, disabled triggers and replica identity drift. Twenty-one rollback-only cases pass, including the missing/wrong primary key and correct-name-but-wrong-key partial index cases. The intact existing LoginRole primary key already prevents duplicate cross-environment binding rows; this closes an attestation gap rather than an observed duplicate-binding incident.

The local full backend checkpoint passed 14 projects and 1,566 tests, with zero build warnings/errors. That snapshot includes the first seven runtime-privilege cases and predates the final six runtime cases and 21 binding cases; do not describe it as full-suite evidence for later source. After the final catalog changes, all 124 affected delivery integration tests pass. The external gate remains unchanged.

The external `SELECT false` gate stays closed. Identity activation still requires full-profile composition, real authentication/step-up flows, and transactional absent-or-exact installation. Other pending prerequisites include complete ACL/return-schema attestation, compatible profile3/profile4 consumers, and transactional installer/postflight tests. No whole-profile or merge-readiness claim follows from the bounded tests.

After published checkpoint `fc45f6e`, the two catalog queries are embedded into the draft `audit_execution_privileges` by `build/update-enrollment-delivery-catalog-slices.ps1`. Run that script after changing either shared query; `-Check` verifies freshness and the integration suite independently checks the generated content. Six new tests prove source equality and composed positive/binding/identity/plan-owner/missing-helper behavior; all 130 delivery integration tests pass after composition. Daybreak accepts this inactive composition checkpoint. The plan role comes from the existing lock helper's owner; its complete body/ACL/role-inventory attestation is still required before activation. The identity policy installer is not yet included in upgrade scripts, so the composed profile correctly remains unsatisfied on the existing v3 installation. Historical v3 scripts and the external closed gate are unchanged.

The next local increment closes the plan-lock bridge checks: exact helper body/metadata and singleton API EXECUTE ACL, five-way role separation, plan ownership and 11-table/2-callee/schema privilege inventory, effective PUBLIC/column/sequence/default-ACL exclusions, and API directory-mutation denies. An owner-only block separately validates the sole Api binding and forbids any plan-owner binding; the shared query remains catalog-only. The two policy callees now pin full metadata including planner support and canonical strictness (`has_environment_membership` is STRICT; `directory_database_access` is not). Daybreak accepts this bounded inactive increment; all 157 delivery tests pass locally after the fixture fix below. Full installation, authentication/step-up compatibility, external attestation and profile version consumer work remain pending.

CI for `237bd2c` passed frontend and Windows Agent but failed six backend tests: the test harness assumed `CONSOLE_TEST_PLAN_LOCK_OWNER` was explicitly set, whereas CI uses the fixture's existing default. `PostgresApiFixture.PlanLockOwner` now snapshots that resolved setting and is shared by provisioning and tests. A constructor-only regression checks default/configured names and snapshot stability while restoring the process environment. This fixes the test configuration mismatch without weakening audits. New published-head CI is required; earlier local passes must not be described as passing Linux CI.

The fixture fix at `83440916a425cfc55965ebd8fbc36beebe026b25` subsequently passed both GitHub runs 34693995019 and 34693992946. The next rollback checkpoint exercises the actual draft upgrade in fresh isolated databases using default and C collation. It exposed existing queue functions and a constraint trigger being recreated without upgrade handling. Four unchanged queue functions now use CREATE OR REPLACE to preserve ownership and ACLs; the exact old Outbox constraint trigger is dropped after v3 preflight and recreated within the upgrade transaction. Both tests reach the intended v4 postflight rejection and verify version3, exact binding/reservation rows, absent delivery policies/functions and a real audited worker queue returning NoWork after rollback. All 160 delivery integration tests pass locally. Daybreak accepts this inactive rollback increment; it does not establish successful v4 installation or replace exact-head CI.

Rollback checkpoint `4bc35ffa8f99817ef6f8fcf201b4884674b20a58` passed both GitHub runs 34694586982 and 34694584260. A subsequent disposable candidate composed the staged identity installer and exposed four real upgrade inconsistencies: the role-kind check expected non-pretty parentheses despite requesting PostgreSQL's pretty definition; five execution and three queue wrapper pins still named v3 bodies; dropping the audit function lost its validated EXECUTE ACLs; and the older worker-policy inventory rejected the four separately attested identity policies. The fixes preserve exact semantics: canonical constraint text and v4 body hashes, CREATE OR REPLACE for the audit, and an exception limited to those four policy names on Principals. Daybreak reviewed these bounded changes.

The permanent upgrade test now covers both the uncomposed rejection and a successful staged-identity candidate in both collations. The candidate uses unchanged source files, uniquely checked script markers and unconditional terminal ROLLBACK; it verifies audit OID/owner/ACL equality before the actual postflight, then exact v3 body/data/marker and usable queue after rollback. Temporary diagnostic profile rewrites and log writes were removed from test source. Passing the current internal profile is only composition evidence: external attestation, complete role-pair/consumer/authentication compatibility and activation review remain pending. The actual upgrade still omits identity installation, and the external gate remains false.

All 162 delivery integration tests pass after this composition repair. The scope fixture's extraction endpoint was updated to the immediately following worker-guard DROP because the audit DROP no longer exists; its extracted SQL scope is unchanged. Exact published-head CI is still required for this increment.

The next test-only increment adds the real delivery-pair provision body after the candidate upgrade postflight. Only its uniquely counted BEGIN/COMMIT statements are removed so the outer transaction owns rollback; its real preflight, bindings, grants and postflight remain intact. Both generated LOGIN roles start with empty ownership/ACL dependency sets, and those sets remain empty after rollback. All six targeted upgrade cases pass across default/C collation, including uncomposed rejection, identity composition and complete runtime-pair composition. Daybreak accepts the bounded fixture. The preceding 162-case suite predates these two added cases; no new full-suite count is claimed here.

The internal delivery audit now validates SESSION_USER after the full base audit succeeds. Direct table-owner maintenance remains allowed; other callers require an exact status or delivery scope attestation. The updated body is pinned as `a37b4693ed042c9d9f69555024c87cb5c40705cc60606bff14145b24480b1d9e`. The rollback fixture provisions a second valid execution environment: owner checks accept both, while each environment-1 delivery runtime rejects environment 2 as well as null/zero/random identifiers. A simulated NOLOGIN definer session is also rejected. These are explicitly server-side SESSION AUTHORIZATION checks, not physical LOGIN/pool tests. Daybreak reviewed the change and all 164 delivery integration tests pass locally. The external audit resource and consumers remain closed/unchanged.

## Local external catalog checkpoint — inactive

`build/audit-enrollment-delivery-catalog.sql` now stages a catalog-only outer attestation with one non-null boolean result. It validates the actual session identity, purpose-specific direct grants, seven complete callable contracts, exact owner/helper ACLs, shared ACL dependencies and prohibited effective relation, sequence, large-object and parameter privileges. Generated function contracts are checked by `build/verify.ps1`. Both this query and the draft internal base audit require `session_replication_role=origin` and `lo_compat_privileges=off`; rollback probes verify rejection by the outer query and the real internal delivery audit under both runtime purposes.

The focused eight-test checkpoint passes, including the existing concurrency test once. The earlier full backend snapshot completed 14 projects with 1,633 passing tests and one failing concurrent execution test. Reproduction identified `409 ConcurrentChange`, not a receipt-integrity mismatch. The execution endpoint now allows three total complete attempts with a short cancellable delay, preserving fresh transaction, authorization and receipt validation on every attempt. After the fix, eight consecutive real concurrency repetitions passed; all 165 delivery integration tests and three deterministic retry cases passed together (168 total). The latter cover recovery after two injected failures, exhaustion after three and session revocation between attempts, asserting operation/outbox/audit counts and plan state. Daybreak accepted the bounded retry and GUC deltas. The earlier full run is not a passing result; a new complete run is still required.

The new catalog query remains unconsumed: `build/audit-enrollment-delivery.sql` still returns false. Internal transitive callable ABI, complete definer ACL/data contracts, profile3/profile4 consumer compatibility, transactional installation and physical runtime LOGIN/pool validation remain activation prerequisites. PR42 remains draft.

## Local internal callable checkpoint — inactive

The generated internal block replaces the former partial 13-function delivery check with complete contracts for 19 delivery and journal functions, including permission/scope helpers, immutable/stop/journal validators and the stop-boundary lock. It preserves the existing queue block. Nullable metadata uses null-safe comparison; unexpected overloads of reserved function names are rejected. Generation/check order is catalog slices, internal functions, then outer function contracts. Local checks pass; GitHub rejected the additional workflow check step because its OAuth credential lacks workflow scope. The workflow change was removed from the unpublished commit and preserved locally as `D:\AD\.local\delivery-generated-contract-ci.patch`; existing CI remains unchanged.

Rollback-only mutations cover every selected function's removed settings, planner support hook and leakproof flag, plus body-preserving output ABI changes, argument defaults, extra overloads and retained queue attestation. The final 19-function snapshot passed all 14 backend projects: 1,637 tests, no failures or skips. Published `d079e88` also passed both GitHub runs (34698971477 and 34698969760). Daybreak's static review found no material defect in this bounded inactive change. The preceding published `f8cc2ba` passed both GitHub runs (34697896687 and 34697894711), including backend, frontend and Windows agent jobs.

Before freezing definer ACLs, the ACK journal path needs a separate repair: project only used operation columns in both INVOKER validators, grant five additional operation SELECT columns, and grant operation-scoped SELECT on execution_stops with both permissive and restrictive policies. Deferred triggers were experimentally confirmed to retain the originating definer identity on PostgreSQL 18.6. Use a new profile4 CREATE OR REPLACE fragment after v3 preflight, preserving validator OIDs/owners/ACLs and historical migrations. Current planned totals are 42 column and 14 table privilege rows. This does not replace required real-LOGIN commit/stop-rejection tests or full trigger/data-contract attestation.

## Local journal privilege checkpoint — inactive

`build/enrollment-delivery-journal.sql` now replaces both validators after v3 preflight with equivalent logic and narrow operation projections. The draft upgrade grants the five missing operation columns, SELECT on execution_stops, and EXECUTE on the two already-pinned legacy RLS callees (`has_environment_membership` and `directory_database_access`). Paired operation-scoped SELECT policies for stops are included in exact policy attestation. Actual LOGIN testing exposed the missing policy-callee grant; the test does not remove or weaken the existing PUBLIC policies to hide that dependency.

Three committed disposable-database tests exercise a test-only definer ACK/delete wrapper and canonical deferred journal triggers under a real LOGIN: normal commit succeeds, synthetic stop+issued state fails at commit with 23514, and a missing operation column fails at commit with 42501 naming EnrollmentGrantOperations. Failed commits preserve the envelope and leave no ACK. They verify explicit row_security, source-derived grants and CREATE revocation, zero direct runtime relation/column rights, cross-operation stop invisibility, same-operation visibility, and DB/role cleanup. This proves journal execution only; it does not validate production wrapper authorization, identity bindings or the full installer.

Three additional metadata tests first demonstrated false acceptance of RESET ALL on either public policy callee and removal of argument names with the function body unchanged. Null-safe proconfig/proargnames comparisons now reject them. All 173 delivery integration tests pass after these changes; Daybreak accepted this inactive increment. This snapshot follows the complete 1,637-test checkpoint above and still needs exact-head CI. The next ACL contract is 15 direct functions (4 owned wrappers plus 11 grants), 42 column privilege rows and 14 table privilege rows; effective function EXECUTE additionally allows the separately pinned PUBLIC api_database_session helper. The external gate remains closed and exact ACL/data/trigger inventory plus production-wrapper LOGIN flows remain outstanding.

## Local exact delivery ACL checkpoint — inactive

The unconsumed `audit-enrollment-delivery-definer.sql` checks all 15 direct function ACLs, 42 column privileges and 14 table privileges, schema owner/USAGE, role safety and symmetric ownership/ACL dependencies across databases. It rejects effective extra rights, PUBLIC duplicates (including enrollment_execution schema USAGE), grant options, future ACLs, large objects and parameter ACLs. The composed internal audit now also requires the complete all-grantee ACL set of the four owned wrappers, derived from registered runtime purposes. The transactional upgrade revokes PUBLIC EXECUTE from four legacy trigger-only functions; rollback preserves their identities, owners, ACLs and bodies. Catalog tests include wrong-purpose and unregistered wrapper grantees. All 173 delivery tests passed before the last PUBLIC-schema negative; all six upgrade cases were rerun for that correction. Daybreak identified and reviewed that correction. Previous journal commit `50e2491` passed both CI runs 34699624939 and 34699622929. No workflow file is changed.

A separate real-runtime regression reproduced temporary Roles table shadowing in the historical Owner mapping guard (expected 23514, actual allowed), using synthetic data in a rolled-back transaction. The next repair shares one qualified, fixed-search-path function body between a forward migration and the staged upgrade, without editing historical migrations. Full trigger metadata/attachments and normal-role behavior must be verified. The outer activation gate remains false.

## Local Owner mapping guard checkpoint — inactive delivery

The reproduced temp-Roles bypass is repaired by a shared, versioned `owner-mapping-guard-v1.sql`, embedded in forward migration `20260912140000_QualifyOwnerMappingGuard` and included after the v3 upgrade preflight. The migration verifies the LF-normalized resource SHA-256 before executing. Historical migration source is unchanged. The INVOKER guard fixes search_path, qualifies public.Roles, and permits only an existing visible non-Owner role (including custom NULL BuiltInKind); Owner, missing and RLS-hidden targets raise generic 23514. The shared fragment revokes PUBLIC EXECUTE. CREATE OR REPLACE preserves OID/owner/trigger dependency.

`audit-owner-mapping-guard.sql` pins full function ABI/body/config/owner/ACL and the exact sole row-level BEFORE INSERT|UPDATE trigger; it is composed into the generated v4 base. Rollback snapshots now include proconfig and attached trigger catalog. Tests prove real runtime hostile temp/search_path Owner insert/update denial, normal role insert/update commit and readback, hidden/missing rejection under otherwise valid write policies, and metadata/trigger drift. The final focused snapshot passed 199 delivery/API guard tests. The preceding full backend run passed the other 13 projects and exposed ten old composition-fixture failures; the corrected generated-source list, shared guard installation and added composition negative all pass in the focused rerun. This is not a claim of a final full-suite pass.

ACL commit `7e608f0` passed PR CI 34700586683, while push CI 34700585036 failed only `ParallelCandidatesConvergeOnPersistedWinner` (expected 2 calls, actual 1). The test store now captures its initial record before releasing the barrier, and its issuer counter is atomic. All 22 executor tests and ten consecutive race-test repetitions pass. Production executor behavior is unchanged by that test correction. Latest changes still require their own CI. Delivery remains inactive; the next bounded step composes the exact definer ACL slice, followed by complete data/trigger attestation and real production-wrapper LOGIN coverage.

## Local composed definer catalog checkpoint — inactive

The exact definer ACL query is now generated into the real internal base immediately after API binding identity, before runtime privilege checks. Its separate generated marker preserves the narrower structural fixture's honest scope; the generator checks both groups atomically and a new source-equality test pins substitutions. Every definer drift case also calls the real audit under both runtime identities and requires exactly one false/ProfileDrift/version4 result. All eight upgrade/generated checks pass (both collations, absent/pair, rollback-to-v3), and the expanded delivery/API guard suite passes all 200 tests. Daybreak found no bounded composition blocker. The external gate and consumer inactivity are unchanged. Next: complete status/journal table constraints, indexes and trigger attachment inventories before installer/consumer activation.

## Local composed status structure checkpoint — inactive

`audit-enrollment-delivery-status.sql` now pins the ordered ten columns, seventeen PostgreSQL18 constraints, two constraint-bound btree indexes, two user triggers and all four RI triggers of the status-to-result foreign key. It rejects incoming foreign keys, defaults/extra columns/collation drift, unenforced or changed constraints, unsafe index snapshot flags, changed trigger events/conditions/enabled states, and extra structures. Referenced-table RI checks are scoped to this FK and preserve unrelated journal triggers. Function bodies, policies and ACLs remain separate composed prerequisites.

The third generated status-structure catalog block is after definer privileges and before runtime privilege checks. The same 28-case mutation source drives isolated catalog tests and both real internal runtime audit identities; six upgrade cases cover default/C collations, absent/pair state and rollback. All 37 targeted cases and the expanded 229 delivery/API guard tests pass, with generator/source equality checks. Prior `98d922f` passed both CI runs 34701668435 and 34701665830; `edbefa3` passed 34701476792 and 34701474935. The external gate remains false. Structure does not prove existing row validity: owner-only status history invariants and the remaining journal-table contracts are still activation prerequisites. Any data audit must prove visibility under a non-superuser/non-BYPASSRLS owner with FORCE RLS, rather than accept an invisible empty set.

## Earlier cloud recheck record

The initial cloud commit `cfe74ec` was not built or tested and must not be treated as completed delivery attestation. Daybreak Blue subsequently blocked that delta: the external audit queried owner-only application tables through a runtime forbidden from reading them, used throwing regprocedure casts for missing wrappers, and prematurely accepted the unfinished profile4 body in consumers with version3-only downstream checks.

The correction removes that premature profile4 acceptance and replaces the embedded delivery audit with an explicit `SELECT false AS is_valid` gate. This keeps the missing resource from preventing compilation without claiming that a privilege audit exists. A resource regression test pins this temporary closed gate; replace it with positive and negative PostgreSQL catalog tests only when the real attestation is complete. Existing execution clients remain version3-only until complete version4 verification is available.

The v3 archive routing in the existing execution installer and v2-to-v3 upgrade, delivery integration resources, and manually edited dependency lockfiles are retained. Lockfiles still require an actual locked restore; JSON parsing is not NuGet verification. Fresh profile4 installation, upgrade, role isolation and all original handoff completion requirements remain pending.

Cloud shell and public Git clone/fetch work. The checked workspace lacks dotnet/psql/container tools; the previous APT attempt failed on restricted setgroups/setuid operations. Local backend execution is unavailable; the subsequent exact-head GitHub Actions evidence is recorded below. Git HTTPS push lacks credentials, but the configured GitHub connector successfully publishes commits and updates the existing branch. After fetching the two remote branches, neither contains AGENTS.md/agent.md/agents.md, and no ancestor workspace instruction file was found; the supplied attachment instructions below apply.

## Objective and authorization

The user subsequently supplied the global working agreement as attachments (`00-給雲端AI.md`, `AGENTS.md`, `config.portable.toml`, `README.md`); all four were read in the cloud session. Follow the supplied Root-led delegation gate, at most three ordinary subagents, read-only Daybreak-only specialist security analysis, and existing human approval boundaries. Reading portable TOML is not installation or a model switch. The uploaded personal configuration is not copied into this public repository.

Cloud PR run 34688824376 for commit `9bafbd60bfc073449cfacce8f23e40523c4815a6` completed the backend locked restore/build and 14 backend projects with 1,442 passing tests (including 542 integration tests and 59 delivery tests), plus successful frontend and Windows jobs. This evidence applies only to that closed-gate checkpoint, not complete profile4 or real enterprise acceptance. The following codec hardening increment adds exact field names/types and rejects additional result sets, with synthetic readers and an owner-helper PostgreSQL integration test; it needs its own exact-head CI evidence.

Codec checkpoint `f157ca44c826b91a63aede7e366daf5cab73a0ab` subsequently passed both PR run 34689361582 and push run 34689360147 (frontend, Windows synthetic checks, backend). PR backend job 103541800727 completed locked restore/build and all 14 projects: 1,475 passing tests, including 91 delivery tests and 543 integration tests. The real owner-helper codec integration case passed. These are closed-gate synthetic results, not public profile4 or enterprise acceptance.

The next pool increment adds a transaction-scoped shared deployment lock before catalog attestation, a separately disposed strict catalog reader, then strict internal `audit_delivery_privileges(environment)` validation. Both readers reject incorrect names/types/values, missing or duplicate rows, and any trailing result set. The runtime does not call the definer-only scope attestor. External purpose-specific ACL attestation must compose with future internal SESSION_USER/environment-pair validation; neither SQL proof is complete yet. The external `SELECT false` gate is unchanged. Added unit cases cover the result schemas; PostgreSQL cases cover closed-gate readiness/operation rejection, rollback lock release, deployment-lock ordering and cancellation. This increment requires its own exact-head CI evidence before being described as verified.

Continue the IT Management Console implementation against all 100 requirements in `docs/requirements/original-request.md`. The latest user instruction is to continue every stage automatically from the local workspace, build/test/fix/document each stage, and merge verified GitHub PRs without asking again. The earlier cloud request is cancelled. Keep the user-facing workflow in the platform: RSAT was background information about their company, not a separate product workflow.

This repository is deliberately public and authorized: `tengyao-pobo/Active-Directory-Management-System`. Publish code and synthetic fixtures only. Never copy local database credentials, databases, certificates, session tokens, or `.local`/`.tools` contents. Do not perform real AD, Entra, IAM, CA, production database, or enterprise changes; these still require exact approval and environment verification. Windows/enterprise-only checks remain explicitly pending while cloud-compatible implementation continues.

Use a small number of agents only for useful independent work; Root may take over poor agent work. Specialist security analysis/review must use Daybreak Blue only; if unavailable, report it and do not substitute a general model for essential specialist review. Ordinary coding/testing can continue independently. Do not create scheduled tasks or claim indefinite background operation without an actual supported scheduler.

## Verified base

- `main` at `d1e0aa35ac4e5b18ad6c1bb1b72e680141c8039f`, PR #41 merged.
- PR #41 head `9df5072601b64e8ae5116e9dc69f23f387c796f8`, tree `b7e4b9918dc3d36d58402479fe5c2a6e708bc6eb`.
- Both push and PR workflows passed: 34687261433 and 34687271894.
- Local full backend: 14 projects, 1,401 passing tests, zero build warnings/errors. Final affected status integration suite: 23 passing tests after two concurrency cases and a test-oracle correction. Do not add these counts together as unique tests.
- PR #40 private lifecycle profile4 isolated status reader/delivery DTOs and PR #39 verify-only worker host are already merged.
- Read `docs/architecture/implementation-status.md`, platform-grant-delivery.md, platform-grant-public-store.md, platform-grant-queue.md, platform-grant-worker.md, and deployment/enrollment-worker.md for context.
- Overall requirements remain incomplete. The coarse status table is 4 I / 58 P / 38 A; the user was told approximately 33% by weighting P as one half, explicitly not an exact effort estimate.

## This branch is an unfinished checkpoint, not merge-ready

`codex/platform-delivery-capabilities` contains incomplete public profile4 integration. Do not deploy, activate readiness, or merge this snapshot just because some earlier tests passed.

Completed bounded pieces:

1. `20260912130000_EnrollmentGrantDeliveryHelpers.cs` adds four owner-only SECURITY INVOKER helpers: read_status_refresh_receipt, lock_delivery_context, get_sealed_delivery, ack_sealed_delivery. Combined with the prior record_status_observation helper there are five owner helpers.
2. Current helper migration SHA-256: `EAD9661859DD2F7209427D1AA21A01B7BF9A01FB280A4CCCC7669F031A842664`. Daybreak approved this exact delta after removing an unnecessary FOR UPDATE from the immutable receipt read.
3. On a fresh isolated PostgreSQL 18 database, all 23 `DeliveryHelper` integration cases passed, including a receipt-read transaction that does not block another connection's ACK. The prior failed ACK tests were a fixture issue: receipt setup forced constraints immediate; tests now restore normal initially-deferred mode before atomic ACK/delete.
4. `EnrollmentGrantStatusObservationWriteResult` strictly normalizes the 12-column record response. Recorded/AlreadyRecorded require exact candidate binding; Terminal returns an existing different terminal observation and does not accept the new UUID; NotFound requires all payload columns NULL. Its project had 58 passing tests including 15 new cases before later repository work. Root changed only Unknown() visibility from private to internal afterward.

Original handoff C# gaps (historical; cloud checkpoints above supersede build/resource/lockfile status):

- EnrollmentGrantDeliveryStores.cs, PostgresEnrollmentDeliveryPool.cs, PostgresEnrollmentDeliveryCodec.cs, PostgresEnrollmentGrantStatusStore.cs, PostgresEnrollmentGrantDeliveryStore.cs.
- New project reference to AgentPlatformGrants and corresponding friend assembly; embedded audit resource `EnrollmentDeliveryCatalogAudit` points to `build/audit-enrollment-delivery.sql`, which is NOT WRITTEN yet. Therefore this checkpoint is expected not to build until the audit file and lockfiles are completed.
- Pool audits before every SERIALIZABLE transaction; unknown decoded results roll back; no accepted result escapes before confirmed commit. Purpose and environment are fixed per pool. Session hashes are uppercase 64-character hex, never raw cookies. Add focused codec/negative/connection/audit/commit-ambiguity tests, review and fix as needed.
- Lockfiles have NOT been refreshed after the new project reference. Integration.csproj still needs delivery reference and copied new SQL scripts for runtime tests.

## Profile4 SQL work in progress

An agent froze work when cloud migration was requested. New scripts: enrollment-delivery-profile.sql, provision-enrollment-delivery.sql, upgrade-enrollment-execution-v3-to-v4.sql. Modified existing scripts: enrollment-execution-functions.sql, enrollment-execution-queue.sql, and a partial enrollment-execution-profile.sql.

These scripts have NOT been parsed, built, or run. The delivery audit currently relies on an INCOMPLETE execution profile4 slice. Complete exact policy expressions, table/column ACLs, owner helpers/table/constraint/trigger metadata, no-extra-privilege checks and the absent/pair matrix. Never treat that draft as proof of security.

Still required:

- Write external `audit-enrollment-delivery.sql` with one boolean row, pin the audit function's body/metadata before trusting it, and validate purpose-specific rights. Exact placeholders agreed with C#: `:'runtime_role'`, `:'delivery_definer_role'`, `:'expected_table_owner_role'`, `:'expected_environment_id'`, `:'expected_purpose'`.
- Finish provision-enrollment-execution.sql, audit-enrollment-execution.sql, audit-enrollment-grant-operations.sql, provision-runtime.sql and API/C# pins/version acceptance. Existing C# must support exactly 3 or 4 for staged upgrade, with complete version-specific audits.
- Keep historical `build/enrollment-execution/v3/` copies immutable. Adapt v2-to-v3 upgrades to use these instead of the new profile4 scripts. Preserve existing v2 archive.
- Implement fresh installation and exact transactional v3 preflight/v4 postflight. Regenerate ALL changed normalized body pins, verify catalog output on PostgreSQL including Linux collation (`COLLATE "C"` where ordered textual definitions are hashed), then test rollback injection, repeat provisioning and complete role/environment separation.
- Public profile4 globally installs a shared NOLOGIN DeliveryDefiner. For each environment, the status/delivery LOGIN pair may be wholly absent or both complete; never a partial pair. All global structure must still audit in the definer-only state.
- Provision parameters: status_runtime_role, delivery_runtime_role, delivery_definer_role, expected_table_owner_role, expected_environment_id, DBNAME.
- Reserve role capability EnrollmentGrantDelivery with DeliveryDefiner/StatusRuntime/DeliveryRuntime kinds. Bind purposes EnrollmentGrantStatusRefresh / EnrollmentGrantDelivery, contract1, PrincipalId NULL. No rebind/reuse with execution/API/private roles. LOGINs get wrappers/audit only, no tables. Shared definer gets exact helpers, minimum columns and scoped RLS. Dummy UPDATE privileges needed for row locks must be paired with audited triggers rejecting actual mutation of authority rows.

Agreed public wrapper signatures (do not accidentally diverge from C#):

- audit_delivery_privileges(uuid) -> is_valid, diagnostic_code, profile_version
- read_grant_status_receipt(uuid,uuid) -> same 14 columns as owner receipt helper
- append_grant_status_observation(same 17 parameters as owner record helper) -> same 12 columns
- read_grant_delivery(uuid,uuid,uuid,text) -> same 10 columns as owner GET
- acknowledge_grant_delivery(uuid,uuid,uuid,text,bytea,bytea) -> same 2 columns as owner ACK

Each wrapper validates exact SESSION_USER purpose/environment binding, acquires shared profile lock, audits, and configures transaction-local `app.delivery_environment_id`, `app.delivery_operation_id`, `app.delivery_requester_id`.

## Approved delivery rules

- GET and ACK both revalidate original requester, enabled principal, active membership, current Ready directory generation Computer, and current Computer.View AND Owner-only AgentEnrollmentGrant.Manage.
- Common helper locks environment -> sync -> current computer -> principal -> membership -> session -> operation; initial operation lookup only identifies immutable target. Re-read locked env/op/requester/directory tuple. Use DB clock after locks.
- Session IdHash is TEXT uppercase hex64, not bytea. Session must be unrevoked/unexpired, canonical finite chronology; StepUpAt and LastSeenAt each no older than ONE minute, not future and not before CreatedAt. This conservative delivery limit cannot exceed any supported global minimum setting (StepUpMinutes >=1, IdleMinutes >=1). Mirror it in future API UX. Do not replace it with fixed5 minutes or bypass step-up for ACK.
- Do not revalidate old approver/plan/approval expiry/issuance authorization deadline to deliver an already issued grant.
- GET only returns ciphertext for latest fresh Available, Issued, envelope present, no ACK, grant unexpired; reject clock rollback before recorded/private-observed time. DTO remains seven public fields and <=15-second deadline.
- ACK JSON remains only formatVersion/recipientKeyFingerprint/ciphertextSha256. Derive token hash from DB permit/receipt. Exact ACK retry succeeds after envelope deletion; original requester must still pass current authorization. ACK can clean up after Unknown/Consumed/Revoked/Expired. Insert ACK and delete envelope in the same transaction; CSRF enforced by future API, not a caller SQL token.
- Unknown status has no invented private timestamp. New Available must be >= maximum Unknown recorded time; terminal status latches. Exact old observation retries are historical evidence, not current delivery permission.

## Cloud verification and next stages

### Public profile4 trust-chain repair required before activation

Daybreak's follow-up identified that the original `delivery_worker_scope` was an invoker function that read the owner-only `role_reservations` table. Granting SELECT alone would not solve its FORCE RLS restriction. The bounded repair now changes the source to a separately pinned, table-owner SECURITY DEFINER attestor with fixed search_path and row_security=on, PUBLIC revoked, and EXECUTE only for the delivery definer. It validates SESSION_USER's exact purpose/environment/binding/reservation, role flags and membership absence, and the unique shared definer reservation; CURRENT_USER is the owner, not the delivery-definer identity. Draft profile expectations change the definer-owned wrapper count from five to four and pin scope ownership/config/body plus the exact owner/definer EXECUTE ACL. The obsolete definer binding-table column grant is removed; draft auditing rejects direct definer table/column rights on both identity tables.

`EnrollmentGrantDeliveryScopeTests` uses rollback-only synthetic role/identity DDL and the exact source scope function. A test-only SECURITY DEFINER wrapper preserves runtime SESSION_USER across the two definer hops; no SET ROLE or membership grants are used for the positive path. Cases cover both purposes, invalid environment/purpose, direct runtime/definer protected-table denial, direct runtime scope denial, and identity/reservation/membership drift. The source-extracted harness is not a complete installer/upgrade test. This scope increment still requires exact-head CI and does not complete the public profile, audit_delivery session binding, absent-or-exact pair matrix, or activation.

The external audit must read pg_catalog only, validate both audit function bodies/metadata before trusting the internal chain, and use to_regprocedure for absent objects. The internal owner audit must prove the complete global structure and per-environment absent-or-exact status/delivery pair before wrappers attest their hard-coded purpose and set transaction-local context. Required negative cases include missing wrapper, altered transitive audit body, purpose crossover, wrong environment, partial pair, altered scope attestor and zero direct runtime table access. The external closed gate must remain until these tests pass on PostgreSQL.

Use the existing `.github/workflows/verify.yml` as the portable environment reference: .NET SDK from global.json, Node/pnpm from workflow, PostgreSQL 18 with psql. Run database projects sequentially because fixtures mutate shared catalogs. Use fresh synthetic public DB and isolated private fixtures; do not recreate local secrets. Backend verification is build/verify.ps1 or equivalent locked restore/build/test -m:1. Frontend has lint, tests, enrollment interop, build, Playwright.

First complete and test this profile4/store increment, request ordinary and Daybreak review, verify exact published head in both CI workflows, and only then merge. Continue status refresh orchestration/pools, authorized GET/ACK API, browser trusted local handoff, durable expired ciphertext disposition/cleanup, listener/CA enrollment and worker activation. Readiness remains false until the full path is safe and validated. Then continue the remaining original platform requirements rather than stopping at enrollment or V1. Keep real Windows/enterprise validation distinct from synthetic code tests.

No current local process or state is needed by the cloud task. Local source and synthetic test data are preserved for recovery; they are not a dependency for cloud continuation.

## Local stop structure checkpoint — inactive

The stop catalog now pins all three columns, eight local constraints, the primary index, the operation FK and its four RI triggers, plus all seven stop-boundary/immutability/deferred trigger attachments. Each deferred trigger is paired with its exact PostgreSQL 18 constraint record. Thirty-three rollback-only drift cases include missing peer tables and renamed validators. The standalone query and separate generated stop-structure block are source-equal; all faults are also rejected by the real internal audit under both delivery purposes and both tested database collations.

Composition exposed three older hard regclass references in execution/queue definer ACL expectations. They now use to_regclass: a missing relation leaves an unmatched expected ACL tuple and produces one false/ProfileDrift/4 result instead of throwing. Daybreak reviewed this bounded repair. All 47 targeted tests and the expanded 263-test delivery/runtime regression suite pass. The preceding published 252ed0c passed both CI runs 34702567943 and 34702565909; current changes require their own published-head CI. The external gate remains false, production consumers remain inactive, and remaining journal structures/data validation plus complete installation and physical production-wrapper LOGIN flows are still required.

## Local envelope structure checkpoint — inactive

The separate generated envelope-structure slice pins two columns, six local constraints, its primary/FK indexes and four RI triggers. It preserves BEFORE UPDATE-only immutability so ACK deletion remains possible, and requires deferred AFTER INSERT OR DELETE journal validation. The full four-table validate_journal attachment set is pinned with reverse constraint bindings; incoming envelope foreign keys are rejected. Thirty-five standalone drift cases and every composed fault pass for both runtime purposes and both collations. Missing envelope/mint catalog lookups now use the same false-on-missing ACL expectation pattern as the preceding stop repair.

Daybreak found no material defect in this bounded inactive increment. All 50 targeted tests and the expanded 299-test delivery/runtime regression suite pass; all three generation checks pass. Full profile activation, successful actual v4 installation/consumers and physical production-wrapper LOGIN acceptance remain pending. This adds no real enterprise mutation or activation authorization.

## Local acknowledgement structure checkpoint — inactive

The generated ACK slice pins all five columns and 13 constraints, including independent NOT NULL/value checks and both deferred trigger records. Its exact three local triggers reject journal/stop validator swaps despite their identical timing. It pins the outgoing FK and constraint-scoped RI triggers while accepting unrelated status-observation RI triggers on the shared parent; incoming ACK foreign keys are rejected. Forty-four standalone mutations and all composed cases pass under both delivery purposes and both database collations. Remaining hard issue_results ACL lookups now use false-on-missing to_regclass expectations.

Daybreak reviewed the bounded change without a material blocker. All 60 targeted tests and the expanded 344-test delivery/runtime regression suite pass; all generated-content checks pass. The external gate remains closed. Next are permit and issue-result structures, activation-time data validation, complete v4 installation/consumer acceptance, and real production-wrapper LOGIN/authentication flows. This checkpoint does not establish merge or production readiness.

## Local permit and issue-result structure checkpoint — inactive

All six journal/status table structures are now composed. The result slice pins 15 columns, 11 constraints including the complete Issued/Rejected closed shape, and the exact unique (environment_id,grant_id) index filtered to Issued. The permit slice pins eight columns, 19 constraints and token uniqueness. Each slice independently validates its outgoing and two allowed incoming foreign keys, all 12 constraint-OID-tagged RI triggers and exactly six local internal triggers. Status observations correctly reference operation_id at child position 3. Results are generated before permits; all dedicated structure blocks precede runtime privileges.

The 68 result and 66 permit standalone cases include weakened branch-specific shape checks, partial-index drift, validated wrong-key status FKs, and PK alterations with both incoming FKs restored. Every mutation is exercised by the real internal audit under both runtime purposes and both collations. This exposed three remaining hard Operations column-ACL references; to_regclass now preserves a single false/ProfileDrift/4 verdict when the relation is renamed. Daybreak reviewed the source, tests and narrow repair without a material blocker.

All 152 targeted tests, 480 delivery/runtime regression tests and three generated-source checks pass. Previous stop and envelope commits passed both CI workflows (0580962: 34703489157/34703487157; afc4191: 34703730709/34703729205). The current checkpoint requires its own CI. These are structural contracts, not proof that existing history rows are consistent. Activation-time history validation with verified unfiltered RLS visibility is next; the external gate, real installer composition and production consumers remain inactive.

## Local history-rule and real-owner prerequisite checkpoint — inactive

The unconsumed `audit-enrollment-delivery-history-rows.sql` stages cross-table journal/stop/status validation, preserving the canonical Unknown barrier, exact Available deadline, wall-clock reversal behavior and Revoked-after-expiry behavior. One disposable-database test exercises 10 valid and 40 invalid histories, with malformed rows seeded only by a privileged test connection and each mutation rolled back. This is explicitly the row-rule layer, not an RLS completeness or activation proof. Daybreak found no material mismatch against the canonical validators.

A second disposable fixture proves a physical LOGIN by a non-superuser, non-BYPASSRLS, NOINHERIT owner can pass the complete composed candidate profile4. It transfers only inventoried local application objects, excludes extension members, preserves exact policy semantics and nonowner function metadata, and verifies no ownership escapes the target database. The short-lived generated login has one connection and a 15-minute password validity; database/role cleanup is verified. This fixture adaptation does not change the real installer or confer enterprise mutation approval.

The 481-case delivery/runtime regression run passed before the final owner fixture; both history tests passed after the fixture was finalized. Exact-head CI remains required. The preceding complete structure checkpoint c3f9ee3 passed both CI runs 34704675498 and 34704672303; acknowledgement checkpoint 8759bdf passed 34703980714 and 34703978457.

The next single-DO activation wrapper must take the shared-profile exclusive advisory lock and strong locks on nine relations (the seven history tables, Environments and DirectoryDatabaseBindings), pin the audit function before invoking it, prove the exact real owner chain and all execution bindings, create/attest seven temporary owner SELECT/true policies, reject every owner-applicable restrictive SELECT/ALL policy, run the row scan and remove/re-audit before success. Keep row_security=on: actual PostgreSQL 18 testing proved row_security=off errors even with an unconditional true policy under FORCE RLS. No wrapper, policy installation, installer consumption or external gate activation is included in this checkpoint.

## Local history visibility wrapper checkpoint — inactive

The unconsumed single-DO history wrapper now locks the shared profile and nine relations, pins the complete base-audit contract before calling it, and validates every execution binding under a physical restricted owner LOGIN. It retains FORCE RLS and row_security=on. Before scanning, it requires the exact owner-applicable policy set: the canonical Operations policy alone, then that policy plus seven temporary owner SELECT/true policies. Both restrictive and additional permissive policies are rejected because arbitrary expressions can have side effects. Noninherited role policies do not confer visibility. Temporary policy names must be unoccupied; successful execution removes all seven policies and re-audits before completion, while exceptions roll back their creation.

The disposable real-owner test proves an otherwise invisible malformed history is detected and checks exact policy snapshots, unchanged RLS settings, and released transaction/relationship locks after success and failure. Negatives cover PUBLIC and inherited policies, occupied names, wrong owner, empty execution bindings, FORCE RLS/configuration drift, altered canonical policy expressions and a substituted base audit. Generated hash and row query equality are enforced by integration tests and build/verify.ps1; all four generator checks pass.

Daybreak reviewed the complete applicable-policy repair without a remaining blocker in this bounded wrapper. The related 483-test regression run passed; the final three history tests passed again after adding the canonical-policy mutation. The preceding c414262 commit passed both CI runs 34705668267 and 34705665638. This checkpoint requires its own published-head CI. Real installation authority/transaction composition, complete pair acceptance, production-wrapper LOGIN/authentication flows and consumer activation remain unfinished. The external gate remains false and PR42 remains a draft. Development continues locally; the earlier cloud-specific execution notes are historical.

## Local readiness transition foundation — inactive

The physical-owner installation boundary requires durable PendingHistoryAudit before a separate owner LOGIN can validate history. The new activation design records the initial two-phase flow, owner-close/installer/owner-audit reinstallation sequence, unresolved privileged-executor versus final-owner authority, and twelve current shared-lock wrappers. All directly granted helpers and API paths still require inventory before activation.

The unconsumed profile4_readiness relation and INVOKER guard stage only the singleton installation generation/nonce/manifest/state shape and legal state transitions. The guard checks the physical restricted owner, acquires the exclusive transaction advisory lock itself, preserves the pending installation tuple when recording Ready, and requires generation increment/new nonce when closing an active installation. It exposes no callable ready setter. The owner remains the database trust root; this primitive alone is not history proof, a runtime gate or an installer.

The real-owner disposable fixture now covers malformed inserts, NULL and immutable-field mutations, incorrect caller, runtime SELECT/INSERT/UPDATE/DELETE/TRUNCATE/trigger-function denial, Ready rollback, reinstallation generation rules and two-connection contention. An owner transition without a caller lock waits behind a runtime shared transaction lock, completes after release, rolls back to Pending and leaves no advisory lock. Daybreak reviewed the corrected guard and tests without a material blocker for this unconsumed foundation. The focused physical-owner test passed after all final edits; its multiple cases remain one xUnit test.

Full backend verification at preceding history commit 3b7af5e passed all 14 projects and 1,954 tests with zero failures/skips; Integration contributed 1,004 tests. Both exact-head CI runs 34706508965 and 34706507305 passed. The foundation requires its own CI. Next are a generated noncircular manifest, exact readiness catalog attestation, owner-only maintenance structural audit separation and ordinary runtime readiness predicate; then compose actual history transitions and all runtime/installation boundaries. The external delivery gate remains false and PR42 remains a draft.

## Local history manifest and readiness predicate — inactive

The generated history-attestation contract manifest hashes eight independent LF-normalized UTF-8 roots with a versioned filename/digest framing. It intentionally excludes generated consumers to avoid a circular hash. This is not a whole-install digest: every installation still requires a new generation/nonce and Pending state even when these roots do not change. Ordinary live profile attestation remains mandatory.

The unconsumed owner-only profile4_ready predicate requires the canonical application owner, restricted LOGIN flags and zero membership edges, a non-RLS ordinary readiness table, exactly one current-manifest Ready row, finite ordered timestamps and matching ready_by. No runtime EXECUTE grant, setter, installer include or production audit consumption is added. Empty/Pending/reclosed state, wrong manifest, wrong ready_by, missing row and RLS drift all return false in the disposable physical-owner fixture. Trigger-disabled row mutations are rollback-only. The actual owner DO performs UPDATE then calls the STABLE predicate as a subsequent statement before commit; that visibility test passes, and rollback restores Pending.

Daybreak found no material blocker for this unconsumed increment, with the manifest scope and remaining live-attestation requirements explicit. Both focused tests passed after the final same-DO case; one independently recomputes the source bundle digest, and the other exercises the complete physical-owner history/readiness fixture. All five generation checks pass. The last full backend result remains the 1,954-test run at 3b7af5e. Next: exact readiness relation/function/ACL catalog contracts and their negative matrix, then maintenance/runtime audit separation and installation integration.

## Local readiness structure and function attestation — inactive

The new unconsumed readiness relation slice pins nine columns, 17 constraints and their raw catalog flags, the sole boolean primary index, the exact BEFORE UPDATE/DELETE trigger attachment, and the complete owner-only relation ACL with no column ACLs. Thirty rollback mutations cover missing/changed relations and functions, RLS/policies, columns/defaults, all nine value/shape checks, index/trigger drift and PUBLIC grants; every rollback rechecks the valid baseline. DEFAULT NULL was replaced with a substantive default mutation because PostgreSQL canonicalizes that no-op away.

A separate catalog-only function slice pins the two zero-argument readiness functions' exact ABI, return kind, owner, language, volatility, security/configuration, overload inventory and complete owner-only EXECUTE ACL. Its generated hashes preserve raw function bodies except CRLF-to-LF normalization, avoiding comment/string whitespace ambiguity. The function-contract generator follows the manifest generator; six generation checks now run in build/verify.ps1. An independent test reconstructs both function contracts.

Daybreak reviewed both bounded catalog slices without a material blocker; the subsequent raw-LF hash change preserves more source information and passed the same tests. Twenty-four function mutations include fixed-success bodies, security/config changes, PUBLIC grants, overloads and wrong return/set kinds. All three focused tests passed after final edits; these exercise both source-equality tests plus the disposable physical-owner fixture with the structural, function, history and transition cases. The foundation d393a01 passed both CI runs 34707377331 and 34707375519. Full backend evidence remains 1,954 tests at 3b7af5e; current changes require their own CI.

These are still separate unconsumed slices. Next is an owner-only maintenance structural audit plus a readiness-gated ordinary audit, preserving the ordinary audit OID/ACL and avoiding a hash cycle. Complete actual installation, all direct-call surfaces, pair/runtime LOGIN acceptance and external gate activation remain pending.

## Local maintenance/runtime audit split candidate — inactive

The generated owner-only INVOKER audit_execution_profile_structure copies the complete canonical structure audit, preserves every reference to the existing ordinary audit ACL, and monotonically embeds both readiness catalog slices. The ordinary audit candidate uses CREATE OR REPLACE to preserve OID/owner/ACL, independently anchors the owner in Environments and role_reservations, pins maintenance's complete ABI/body/ACL before invocation, requires one valid maintenance row, and only then calls the attested readiness predicate. The dependency direction is acyclic; seven generation checks and independent source equality cover the composed body and raw-LF maintenance hash.

The disposable fixture now provisions a real status/delivery pair before its test-only owner transfer. Actual execution/status/delivery LOGINs all observe false ordinary audits while Pending; all twelve production wrappers have EXECUTE privilege but reject calls with 42501 in their required SERIALIZABLE transactions. This avoids false success from missing privileges or missing transaction prerequisites. After the trusted test-only Ready transition, all three purpose audits return true and the real queue claim path returns NoWork. Ordinary audit OID, owner and ACL remain exactly equal to the original snapshot.

Nine rollback drifts cover missing/overloaded/altered/granted maintenance, unpinned exception-raising maintenance/ready bodies, and disabled readiness guard. The wrapper returns false without invoking the unpinned sentinels, and every rollback restores a positive audit. All four focused tests passed after the final twelve-entrypoint expansion; these are three source-equality Facts plus the full physical-owner fixture. Daybreak reviewed the split call graph and first runtime proof without a material blocker; the final fixture expansion retains the same reviewed SQL. The manifest checkpoint e81ee42 passed both CI runs 34707693461 and 34707691496.

This is still an unconsumed candidate, not a successful actual installer/history activation. The Ready transition above is explicitly a trusted test primitive. Next: pin both maintenance and ordinary audit inside a candidate history DO, validate expected pending generation/nonce/digest, scan/remove policies/re-audit maintenance, transition Ready, then attest ordinary audits for all bindings before commit. Full GET/ACK authentication flows, installation authority/postflights, all direct helper/API surfaces and external gate activation remain pending.

## Local owner history activation transaction — inactive

The unconsumed activate-enrollment-profile4-candidate.sql now carries typed expected generation, nonce and history-contract digest through a transaction-local temporary table. Its generated single-DO body pins both maintenance and ordinary audits, requires a physically authenticated restricted owner with no memberships, and verifies the exact Pending tuple under the profile and relation locks. It preserves the previously reviewed temporary-policy visibility and complete history scan, removes those policies, re-audits maintenance, conditionally records Ready and verifies ordinary audits for every execution binding in a subsequent statement before commit.

The disposable owner fixture executes the unchanged bootstrap with real psql owner LOGINs. A correct tuple commits Ready; a wrong generation or repeated activation fails without altering state. Rollback cases cover missing/empty/duplicate carriers, wrong nonce/digest, malformed otherwise invisible history, final-audit failure, occupied temporary names and an owner with CREATEDB. Cleanup compares exact state and policy snapshots and checks temporary objects and advisory locks. The test-only CREATEDB restoration uses an uncancelled token with a bounded command timeout.

Daybreak accepted this inactive checkpoint without a material blocker. All 487 related regression tests passed, then all five focused tests passed after the final cleanup edit. Eight generated-content checks pass, including independent source equality for the exact preserved history block and both raw-LF audit body pins. The preceding c67433c passed both CI runs 34708814250 and 34708811996; this checkpoint still needs its own exact-head CI. The last full backend run remains 1,954 tests at 3b7af5e.

This is a tested activation candidate, not a composed production installer. Existing privileged installation/final-owner authority, complete direct-call inventory, consumer and external catalog acceptance, and full production authentication flows remain pending. The external delivery gate is still false; PR42 remains draft. Continue with explicit privileged executor versus restricted final-owner installation boundaries and fail-closed reinstallation.

## Local effective runtime surface checkpoint — inactive

The real execution/status/delivery LOGIN fixture now enumerates all effective private-schema function permissions, requiring exactly 9/3/3 functions including audits and rejecting extra overloads. Separate catalog scans require no effective table/view/foreign-table/column privileges or sequence USAGE/SELECT/UPDATE. The final full owner fixture passed after these additions. Sequence scans use CASE to restrict type-specific privilege evaluation; an initial AND-only query exposed PostgreSQL predicate reordering against a non-sequence and was corrected before commit.

Daybreak confirmed this evidence closes the three-purpose helper inventory only, not the API public-schema publication path. The activation design now records a trusted privileged phase-1 executor, restricted final owner, exact ownership manifest, Pending-only commit and separate physical owner history activation. API direct plan/operation/outbox publication still needs a database barrier and shared-lock ordering. No installer, runtime gate or migration is activated by this test/documentation checkpoint.

## Local stale-snapshot closure checkpoint — inactive

A new real queue regression first failed because an old SERIALIZABLE Ready snapshot survived the owner's committed Pending transition. The repaired candidate makes both ordinary audits VOLATILE. Execution audit tries the shared advisory transaction lock before catalog access, pins maintenance, locks the singleton readiness row FOR SHARE, then evaluates the attested Ready predicate. This detects stale rows with 40001 and returns the existing false verdict for a fresh Pending or contended direct audit. All twelve wrappers retain their current ordered lock/audit calls and exact callable grants.

The expanded owner fixture covers stale direct execution audit, queue, status and delivery calls, followed by fresh Pending rejection; direct audit during owner-exclusive close proves no wait/no readiness row lock; owner history succeeds reentrantly. A new metadata slice pins both audit ABIs, owners, VOLATILE/configuration and no-overload shape. Volatility, missing configuration and extra overload mutations are rejected. Generated history pins the updated runtime body. The separate external catalog still pins the old candidate and remains unconsumed; it must be composed before any positive external activation claim.

API publication now acquires the shared profile lock before public-context/plan locks. The existing API suite passed 91 tests including the new profile-lock step-up-expiry case; a separate added processor-close-after-wait test also passed. The 487-test related regression run passed after the core repair. Final five focused tests cover the complete owner fixture and source contracts after the last metadata mutation/comment additions. Eight source-generation checks pass. Daybreak's bounded code review found no material blocker for this inactive correction. Prior checkpoints 6d54f2e and 50485a9 passed both CI runs (34709809095/34709807941 and 34710106522/34710103377); this checkpoint requires its own exact-head CI.

Next: operation-INSERT publication guard with a nonwaiting shared lock and full locking-audit pin; exact existing queued-anchor contract supplies Plan/Outbox completeness. Use an independent guard template with one runtime hash placeholder and dynamically attest its rendered hash linkage to avoid cycles. The actual v3-to-v4 replacement must later be tested against pre-existing runtime snapshots/prepared calls. Installation ownership manifest, public API catalog acceptance, external consumer integration and real authentication flows remain pending; PR42 stays draft and the gate false.
